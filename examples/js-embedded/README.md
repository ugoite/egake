# Embedded JavaScript host

This example shows the Egake-side `ResourceProvider` boundary. The provider owns
data access and actions; an Ikasue host receives only `IkaView` properties and
semantic DOM events such as `ika-query` and `ika-edit`.
