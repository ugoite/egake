# Third-party notices

ikashita is distributed under the MIT License; see [`LICENSE`](LICENSE).

The specification parser uses these direct dependencies:

| Crate | Locked version | License | Use |
| --- | ---: | --- | --- |
| `kdl` | 6.5.0 | Apache-2.0 | KDL v2 document parsing and source spans |
| `serde_json` | 1.0.151 | MIT OR Apache-2.0 | Owned state and component metadata values |

The lockfile also records their transitive dependencies and is the
authoritative version list for a release. Their permissive license texts and
copyright notices remain available from the corresponding crates.io package
sources and must be included by any distribution that vendors those crates.
`deny.toml` provides the baseline license and source policy.
